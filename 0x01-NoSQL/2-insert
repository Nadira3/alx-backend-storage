// inserts a document in the collection school
const result = db.school.bulkWrite([
    { insertOne: { document: { name: "Holberton school" } } }
])
print(`WriteResult({ "nInserted" : ${result.insertedCount} })`);
